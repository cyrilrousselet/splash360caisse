// @flow
import { connect } from 'react-redux'
import Depenses from '../components/Depenses';


const mapStateToProps = (state) => {
  return {}
}

const mapDispatchToProps = (dispatch) => {
  return {}
}

const DepensesCont = connect(
  mapStateToProps,
  mapDispatchToProps
)(Depenses);

export default DepensesCont;